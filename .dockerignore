# Всё, чего не должно быть в образе. Каждая строка — либо лишний вес на карте
# памяти, либо утечка.

.git
.venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache

# Секреты и данные. Попади они в образ — уехали бы вместе с ним куда угодно.
.env
*.db
*.db-wal
*.db-shm
data/
backups/

# Тесты и инструменты разработки в бою не нужны и весят.
tests/
scripts/
docs/
*.md
!README.md
