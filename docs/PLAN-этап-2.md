# Этап 2: Публикация — план реализации

**Цель:** система публикует в закрытую тестовую группу ВК пост с настоящей
обложкой. LLM и генератор картинок остаются заглушками — проверяется публикация
и сборка обложки, а не качество контента.

**Что уже проверено на живом API:** вся цепочка ВК работает, но требует двух
ключей. Детали, коды ошибок и тупики — в `docs/ВК-как-это-работает.md`.
Читать до первой строки кода.

---

## Решения, зафиксированные до начала

| # | Решение | Обоснование |
|---|---------|-------------|
| 1 | Два ключа в конфиге: `vk.token_env` (сообщества) и `vk.upload_token_env` (пользователя) | ни один ключ не проходит цепочку целиком |
| 2 | Шрифт — PT Sans Narrow Bold в `factory/compose/fonts/`, лицензия OFL рядом | системные шрифты macOS проприетарные; узкий русский шрифт нужен под референс владельца |
| 3 | Ретраи на `getWallUploadServer`, загрузке и `saveWallPhoto`; на `wall.post` — **ни одного** | первые три идемпотентны, публикация нет |
| 4 | `guid` генерируется и сохраняется в базу **до** вызова `wall.post` | закрывает окно дубля из «Задолженности Этапа 2» |
| 5 | Пустой `photo: ""` от сервера загрузки — не ошибка, а повод повторить с новым `upload_url` | наблюдалось на живом API дважды |
| 6 | ~~После публикации чистим служебный альбом через `photos.delete`~~ | **не вышло**: метод тоже запрещён не-standalone приложению. Гасим после первого отказа |
| 7 | Обложка собирается поверх сгенерированной картинки, а не вместо неё | картинка — фон, рамка и плашка — оформление |
| 8 | Текст на обложке рисуется только локально | нейросети портят кириллицу; требование спеки |

---

## Карта файлов

| Файл | Ответственность |
|------|-----------------|
| `factory/compose/__init__.py` | пусто |
| `factory/compose/cover.py` | сборка обложки на Pillow по шаблону |
| `factory/compose/fonts/PTSansNarrow-Bold.ttf` | шрифт обложки |
| `factory/compose/fonts/LICENSE-PTSans.txt` | OFL, обязательна при распространении |
| `factory/providers/publishers/vk.py` | боевой публикатор: два ключа, четыре шага |
| `factory/core/steps/compose.py` | переписать: вызывает сборку вместо проверки |
| `factory/core/config.py` | добавить `upload_token_env`, проверку пары ключей |
| `migrations/002_publish_guid.sql` | колонка `posts.publish_guid` |
| `projects/demo/templates/red_frame.json` | уточнить под реальную отрисовку |
| `tests/test_cover.py` | сборка обложки |
| `tests/test_vk_publisher.py` | публикатор на моках |
| `scripts/mutations/cover.json`, `vk.json` | мутационные проверки |

---

## Задачи

### Задача 1: сборка обложки

**Файлы:** `factory/compose/cover.py`, `tests/test_cover.py`

- [x] Тест: длинный кириллический заголовок (60 символов) помещается в плашку —
      требование спеки. Проверять по фактическим границам текста, а не «не упало»
- [x] Тест: короткий заголовок рисуется крупнее длинного (автоподбор кегля)
- [x] Тест: перенос по словам, слово не разрывается посередине
- [x] Тест: результат — PNG 1080×1350
- [x] Тест: рамка нарисована по периметру заданным цветом и толщиной
- [x] Тест: плашка на месте, под текстом белый фон
- [x] Тест: одинаковый вход даёт одинаковый выход (идемпотентность)
- [x] Тест: заголовок из одного очень длинного слова не ломает сборку
- [x] Тест: битый шаблон даёт `FactoryError` с указанием файла
- [x] Реализовать, прогнать мутации

### Задача 2: шаг compose

**Файлы:** `factory/core/steps/compose.py`, `tests/test_transitions.py`

- [x] Тест: шаг перезаписывает файл обложки собранной версией
- [x] Тест: повторный вызов не пересобирает (идемпотентность по метке в базе)
- [x] Тест: inline-картинки не трогаются
- [x] Реализовать

### Задача 3: схема и конфиг под два ключа

**Файлы:** `migrations/002_add_guid.sql`, `factory/core/config.py`, `tests/test_config.py`

- [x] Миграция: `ALTER TABLE posts ADD COLUMN publish_guid TEXT`
- [x] Тест: миграция применяется на базе с данными, версия становится 2
- [x] Конфиг: `vk.upload_token_env`, обязателен при `publisher.provider: vk`
- [x] Тест: `provider: vk` без `upload_token_env` даёт понятную ошибку
- [x] Тест: `provider: stub` без него проходит

### Задача 4: боевой публикатор ВК

**Файлы:** `factory/providers/publishers/vk.py`, `tests/test_vk_publisher.py`

Все тесты на `httpx.MockTransport` — настоящая сеть в тестах запрещена.

- [x] Тест: полная цепочка из четырёх вызовов в правильном порядке
- [x] Тест: загрузка идёт ключом пользователя, `wall.post` — ключом сообщества
- [x] Тест: `photo: ""` от сервера загрузки → повтор с новым `upload_url`
- [x] Тест: `wall.post` **не повторяется** при таймауте
- [x] Тест: `getWallUploadServer` повторяется при таймауте
- [x] Тест: `guid` передаётся в `wall.post` и берётся из базы
- [x] Тест: истёкший ключ (ошибка 5) даёт `FactoryError` с инструкцией обновить
- [x] Тест: ошибка 27 на загрузке объясняет, что перепутаны ключи
- [x] Тест: после публикации вызывается `photos.delete`
- [x] Тест: сбой `photos.delete` не роняет публикацию
- [x] Реализовать, прогнать мутации

### Задача 5: приёмка на живой группе

- [x] Обновить ключ загрузки (живёт сутки)
- [x] Прогнать `factory run --once` до публикации
- [x] Посмотреть глазами: обложка с заголовком, четыре картинки, текст
- [x] Убедиться, что повторный тик не публикует второй раз

### Задача 6: документация

- [x] `RUNBOOK.md`: как обновить ключ загрузки, что делать при ошибке 5
- [x] `README.md`: раздел про два ключа
- [x] `TROUBLESHOOTING.md`: ошибки 5, 27, 214, пустой ответ загрузки
- [x] `CLAUDE.md`: закрыть задолженность про `guid`

---

## Что НЕ входит

Реальные LLM и генератор картинок (этапы 3–4), Telegram-бот (этап 5),
воркер комментариев (этап 6), Docker (этап 7).

---

## Что выяснилось только на живой группе

Ни один из этих случаев нельзя было увидеть на моках.

1. **`photos.delete` тоже запрещён** не-standalone приложению — ошибка 15, та же
   причина, что и у `wall.post` для ключа пользователя. Уборка оригиналов
   невозможна; провайдер гасит её после первого отказа, чтобы не давать по два
   обречённых вызова и два предупреждения на каждый пост.

2. **Сервер загрузки сбоит часто.** За три запуска подряд — дважды: один раз
   пустым полем `photo`, один раз ответом не-JSON. Оба случая заложены в код как
   штатные, а не исключительные.

3. **Дневной лимит отработал сам собой:** третий пост встал в ожидание, потому
   что два уже вышли. Никаких правок не потребовалось.
