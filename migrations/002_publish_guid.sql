-- Идентификатор операции публикации, чтобы повтор не создавал второй пост.
--
-- Пишется в базу ДО вызова wall.post и передаётся в него параметром guid. Если
-- ответ не доехал, а пост уже создан, следующая попытка с тем же guid вернёт
-- существующий пост вместо второго. Это закрывает окно дубля между отправкой и
-- записью external_id.
--
-- Обычное добавление колонки: CHECK-ограничения не трогаем, пересборка таблицы
-- не нужна.

ALTER TABLE posts ADD COLUMN publish_guid TEXT;
