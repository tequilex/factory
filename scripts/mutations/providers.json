{
  "module": "factory/providers/registry.py",
  "tests": [
    "tests/test_providers_stub.py"
  ],
  "mutations": {
    "фактчек ходит на модель для текстов": [
      "    model = (llm.factcheck_model or llm.model) if factcheck else llm.model",
      "    model = llm.model"
    ],
    "перепутать цены текста и фактчека": [
      "        (llm.factcheck_price_input_per_1m, llm.factcheck_price_output_per_1m)\n        if factcheck\n        else (llm.price_input_per_1m, llm.price_output_per_1m)",
      "        (llm.price_input_per_1m, llm.price_output_per_1m)\n        if factcheck\n        else (llm.factcheck_price_input_per_1m, llm.factcheck_price_output_per_1m)"
    ],
    "не брать лимит токенов из конфига": [
      "        max_tokens=llm.max_tokens,",
      "        max_tokens=512,"
    ],
    "не брать температуру из конфига": [
      "        temperature=llm.temperature,",
      "        temperature=1.0,"
    ],
    "терять прокси": [
      "        proxy_env=llm.proxy_env,",
      "        proxy_env=None,"
    ],
    "терять имя переменной с ключом": [
      "        key_env=llm.api_key_env,",
      "        key_env=\"LLM_API_KEY\","
    ]
  }
}
