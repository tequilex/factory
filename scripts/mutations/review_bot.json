{
  "module": "factory/bot/review_bot.py",
  "tests": [
    "tests/test_bot_topics.py",
    "tests/test_edits.py",
    "tests/test_review_bot.py",
    "tests/test_waiting.py"
  ],
  "mutations": {
    "пускать кого угодно к кнопкам": [
      "    return user_id in project.telegram.reviewers",
      "    return True"
    ],
    "не проверять, чей это проект": [
      "    project = projects.get(slug or \"\")\n    if project is None or project.telegram is None:\n        return False",
      "    project = next(iter(projects.values()), None)\n    if project is None or project.telegram is None:\n        return False"
    ],
    "молча игнорировать постороннего": [
      "            await query.answer(NOT_YOURS, show_alert=True)\n            return",
      "            return"
    ],
    "применять испорченную кнопку наугад": [
      "        if parsed is None:\n            await query.answer(\"Кнопка испорчена.\", show_alert=True)\n            return",
      "        if parsed is None:\n            parsed = (1, Decision.APPROVE)"
    ],
    "не сообщать, что решение уже принято": [
      "            await query.answer(_refusal(conn, post_id, decision), show_alert=True)",
      "            await query.answer(\"\")"
    ],
    "не записывать, кто нажал": [
      "        if not apply(conn, post_id, decision, by=query.from_user.id, version=version):",
      "        if not apply(conn, post_id, decision, by=None, version=version):"
    ],
    "оставить кнопки после решения": [
      "        await _replace_keyboard(query, decision, post_id, version or 1)",
      "        pass"
    ],
    "не объяснять, что будет дальше": [
      "        sent = await query.message.answer(text)",
      "        sent = None"
    ],
    "ронять решение из-за неубранных кнопок": [
      "    except Exception as exc:  # noqa: BLE001 — решение уже применено, это косметика\n        log.info(\"не удалось поменять кнопки\", extra={\"reason\": str(exc)})",
      "    except Exception as exc:  # noqa: BLE001\n        raise"
    ],
    "показывать статус чужих проектов": [
      "        if project.telegram and user_id in project.telegram.reviewers",
      "        if project.telegram"
    ],
    "не считать свободные темы": [
      "            f\"  свободных тем: {free}\"",
      "            f\"  свободных тем: {0}\""
    ],
    "считать ждущими постами что попало": [
      "            \"SUM(state = ?) AS waiting, \"",
      "            \"SUM(state != ?) AS waiting, \""
    ],
    "обещать публикацию при истёкшем ключе": [
      "    if alerts.is_raised(conn, \"vk_token\", slug):",
      "    if False:"
    ],
    "не давать ссылку при истёкшем ключе": [
      "        link = alerts.vk_token_url(project.vk.app_id)",
      "        link = None"
    ],
    "не называть время публикации": [
      "return f\"✅ Уходит в группу {when:%d.%m в %H:%M} — это ближайший слот расписания.\"",
      "return \"✅ Уходит в группу в ближайший слот расписания.\""
    ],
    "править пост без проверки прав": [
      "    if not _may_press(projects, slug, message.from_user.id):\n        await message.answer(NOT_YOURS)\n        return\n\n    edit = edits.parse",
      "    edit = edits.parse"
    ],
    "молчать про непонятный ответ": [
      "    if post_id is None:\n        await message.answer(\n            \"Не понял, к какому посту это относится. Ответьте на сообщение с \"\n            \"постом — тем, под которым кнопки.\"\n        )\n        return",
      "    if post_id is None:\n        return"
    ],
    "убирать кнопки совсем после одобрения": [
      "    if decision is Decision.APPROVE:\n        markup = _as_markup(cancel_keyboard(post_id, version))",
      "    if False:\n        markup = _as_markup(cancel_keyboard(post_id, version))"
    ],
    "не возвращать кнопки после отмены": [
      "    elif decision is Decision.CANCEL:\n        markup = _as_markup(review_keyboard(post_id, version))",
      "    elif False:\n        markup = _as_markup(review_keyboard(post_id, version))"
    ],
    "оставлять кнопки после отката": [
      "    markup = None\n    if decision is Decision.APPROVE:",
      "    markup = _as_markup(review_keyboard(post_id))\n    if decision is Decision.APPROVE:"
    ],
    "путать вышедший пост с решённым": [
      "    if decision is Decision.CANCEL and row[\"external_id\"]:",
      "    if False:"
    ],
    "одобрять последний вариант вместо выбранного": [
      "        if not apply(conn, post_id, decision, by=query.from_user.id, version=version):",
      "        if not apply(conn, post_id, decision, by=query.from_user.id, version=None):"
    ],
    "убирать кнопку с отложенного варианта": [
      "    elif decision in (Decision.TEXT, Decision.SCENES, Decision.IMAGES):\n        markup = _as_markup(variant_keyboard(post_id, version))",
      "    elif False:\n        markup = _as_markup(variant_keyboard(post_id, version))"
    ],
    "оставлять кнопку и на выброшенном посте": [
      "    elif decision in (Decision.TEXT, Decision.SCENES, Decision.IMAGES):",
      "    elif True:"
    ],
    "вести кнопку варианта на текущий": [
      "        markup = _as_markup(variant_keyboard(post_id, version))",
      "        markup = _as_markup(variant_keyboard(post_id, version + 1))"
    ],
    "добавлять темы без подтверждения": [
      "    preview = \"\\n\".join(f\"• {line}\" for line in lines[:5])",
      "    topics.add(conn, 1, lines)\n    preview = \"\\n\".join(f\"• {line}\" for line in lines[:5])"
    ],
    "не забывать список после подтверждения": [
      "        conn.execute(\"DELETE FROM meta WHERE key = ?\", (key,))",
      "        pass"
    ],
    "добавлять темы при отказе": [
      "    if parts[1] == \"no\" or row is None:",
      "    if row is None:"
    ],
    "пускать постороннего к темам": [
      "async def _apply_pending_topics(\n    conn: sqlite3.Connection, projects: dict[str, ProjectConfig], query: CallbackQuery\n) -> None:\n    if not _mine(projects, query.from_user.id):\n        await query.answer(NOT_YOURS, show_alert=True)\n        return",
      "async def _apply_pending_topics(\n    conn: sqlite3.Connection, projects: dict[str, ProjectConfig], query: CallbackQuery\n) -> None:\n    if False:\n        await query.answer(NOT_YOURS, show_alert=True)\n        return"
    ],
    "пускать постороннего к паузе": [
      "    mine = _mine(projects, user_id)\n    if not mine:\n        return NOT_YOURS\n\n    for slug, _ in mine:\n        topics.set_paused(conn, slug, paused)",
      "    mine = list(projects.items())\n    for slug, _ in mine:\n        topics.set_paused(conn, slug, paused)"
    ],
    "не снимать тревогу о кончившихся темах": [
      "    alerts.clear(conn, \"no_topics\", payload[\"slug\"])",
      "    pass"
    ],
    "показывать чужие темы": [
      "        if project.telegram and user_id in project.telegram.reviewers\n    ]",
      "        if project.telegram\n    ]"
    ],
    "обещать публикацию сверх дневного лимита": [
      "    if published >= allowed:",
      "    if False:"
    ],
    "не говорить, сколько тем не поместилось": [
      "    return [f\"  …и ещё {total - shown}\"] if total > shown else []",
      "    return []"
    ],
    "не показывать запас": [
      "        lines.append(f\"\\n📋 В запасе ({counts.free}), по очереди:\")",
      "        lines.append(\"\")"
    ],
    "не показывать отработанные темы": [
      "    finished = topics.done(conn, project_id)",
      "    finished = []"
    ],
    "не показывать, что в работе": [
      "    working = topics.in_progress(conn, project_id)",
      "    working = []"
    ],
    "не запоминать сообщение «делаю»": [
      "        _remember_waiting(conn, post_id, getattr(sent, \"message_id\", None))",
      "        pass"
    ],
    "запоминать ожидание и для одобрения": [
      "    if decision in REMAKES:",
      "    if True:"
    ],
    "не обещать срок возврата": [
      "            \"Вернусь через пару минут с новым вариантом.\\n\\n\"\n            \"Этот вариант никуда не делся: кнопка под ним осталась.\"\n        ),\n        Decision.SCENES: (",
      "            \"Этот вариант никуда не делся: кнопка под ним осталась.\"\n        ),\n        Decision.SCENES: ("
    ],
    "выбрасывать пост без переспроса": [
      "    if len(parts) < 3 or parts[2] not in (ASK_TRASH, KEEP):\n        return False",
      "    if True:\n        return False"
    ],
    "пускать постороннего к переспросу": [
      "    slug = _project_of_post(conn, post_id)\n    if not _may_press(projects, slug, query.from_user.id):\n        await query.answer(NOT_YOURS, show_alert=True)\n        return True",
      "    slug = _project_of_post(conn, post_id)\n    if False:\n        await query.answer(NOT_YOURS, show_alert=True)\n        return True"
    ],
    "не давать пути назад из переспроса": [
      "    if parts[2] == ASK_TRASH:\n        await query.answer()\n        await _replace_markup(query, trash_keyboard(post_id, version))\n    else:\n        await query.answer(\"Оставляю.\")\n        await _replace_markup(query, review_keyboard(post_id, version))",
      "    await query.answer()\n    await _replace_markup(query, trash_keyboard(post_id, version))"
    ]
  }
}
