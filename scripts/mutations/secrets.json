{
  "module": "factory/core/secrets.py",
  "tests": [
    "tests/test_vk_token_refresh.py"
  ],
  "mutations": {
    "не давать файлу обновлять ключ, которым система управляет": [
      "        ours = name in _FROM_FILE or name in managed",
      "        ours = name in _FROM_FILE"
    ],
    "позволить файлу перебивать всё подряд": [
      "        if name in os.environ and not (refresh and ours):",
      "        if False:"
    ],
    "не помечать имя как управляемое": [
      "    managed = sorted(_managed_in(existing) | {name})",
      "    managed = sorted(_managed_in(existing))"
    ],
    "стирать пометки владельца при записи": [
      "        out.append(raw)\n\n    if not replaced:",
      "        continue\n\n    if not replaced:"
    ],
    "не схлопывать повторы имени": [
      "                if replaced:\n                    continue  # повтор того же имени — схлопываем в одну строку",
      "                if False:\n                    continue"
    ],
    "оставлять файл читаемым для всех": [
      "        os.chmod(handle.name, _PRIVATE)",
      "        os.chmod(handle.name, 0o644)"
    ],
    "не убирать временный файл при сбое": [
      "        Path(handle.name).unlink(missing_ok=True)\n        raise",
      "        raise"
    ],
    "принять значение с переносом строки": [
      "    if \"\\n\" in value:",
      "    if False:"
    ]
  }
}
